#include <utils.hpp>

void processInt(std::string input)
{
    std::cout << "int : ";
    try {
        // TODO
        // Check if there is a sign.
        // Check if it's a valid sign.
        // Check if there is any alphabtics

    } catch (const std::exception& e) {
        std::cout << "impossible" << std::endl;
    }
}